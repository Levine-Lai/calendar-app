# 项目协作规则

- 只有当用户明确说“打包”或明确要求生成 APK/AAB 时，才执行 Android 打包、签名验证或创建安装包文件。
- 用户说“发布这个新版本到 GitHub”“发布新版到 GitHub”或同义表达时，视为明确要求完整发布：生成并验证固定签名 APK、创建 GitHub Release、上传安装包、更新远程版本清单，并确保 App 内“检查更新”可以发现并打开该 Release 下载页。
- 如果下一个版本的开发工作已经完成，但用户尚未明确要求打包，不要自行打包；应告诉用户下一个版本号，并说明源码已完成、等待打包指令。
