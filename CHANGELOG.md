# 观赛日记版本更迭说明

这份文件写给使用用户看，用来说明每个版本更新了什么。以后每次发布或准备新版本，都在这里继续更新。

## 当前版本状态

- 本机最新已生成 APK：`2.3.2 / versionCode 44`
- 当前源码版本：`2.3.3 / versionCode 45`（源码完成，未打包）
- APK 文件：`releases/sports-calendar-2.3.2-debug.apk`
- GitHub Release：`https://github.com/Levine-Lai/calendar-app/releases/tag/v2.3.2`
- App 内检查更新清单指向 `2.3.2` 的 GitHub Release 页面，兼容旧版本手机浏览器打开方式。

## 2.3.3（源码完成，未打包）

- App 内“打开下载页”会先解析系统默认浏览器的应用包名，再把 GitHub Release HTTPS 地址明确交给该浏览器打开，避免被 App 内 WebView、GitHub 客户端或安装器接管；没有设置默认浏览器时仍回退到系统 HTTPS 选择流程。
- 加深日历中“今天”整格的粉色背景，并增加深玫红内侧轮廓；今天的日期数字框改为更深的玫红底色与白字，使当前日期在有比赛和无比赛时都更容易识别。
- 修复桌面组件1点击刷新后长时间没有实时比分的问题：一次性刷新改为加急 WorkManager，周期任务升级时更新原有调度；比分写入后先立即重绘组件，不再等待缺失队徽逐个下载完毕。
- ESPN 赛事 ID 发生调整或旧赛程缺少 ID 时，组件会按客队与主队的完整名称兜底匹配当天赛事；Web 向原生同步时也会保留赛事自身的 `sport` 和 `espnLeague` 字段，避免旧数据失去实时比分数据源。
- 组件默认日期迁移提升到第 3 版：新组件继续默认北京时间今日，仍停在旧版默认“明日”的现有组件会一次性迁移回今日，其他手动选择日期保持不变。
- `public/version.json` 继续发布 `2.3.2 / versionCode 44`，不会让已安装版本检测到尚未生成的 2.3.3 安装包。

## 2.3.2（已发布）

- 移除全屏奔跑开屏动画，将原 GIF 改为左上角菜单按钮；从桌面、组件、通知或后台恢复时不再重复播放遮罩动画。
- 暂时隐藏“赛程管理”和“智能添加赛程”入口，但保留本地数据、解析器与 Cloudflare 接入代码，后续可以直接恢复而无需重做。
- 阿森纳新闻抓取升级为完整正文：Arsenal.com 优先读取页面内 `articleBody`，并以正文 DOM 兜底；The Guardian RSS 条目会继续抓取公开文章页正文，未取得正文的 RSS 摘要不会展示。本次已重新生成 14 篇官网文章的内置缓存，中英文段落数量一致。
- 新闻详情标题上方保留 12px 呼吸空间。
- 修复部分 vivo 手机无法打开更新下载页：GitHub APK 直链先转换为对应 Release 页面，再由 Android 默认 HTTPS 浏览器直接打开，避免被安装器错误拦截。
- 取消北京时间 00:00–08:59 新闻推送限制；服务端 FCM、本机轮询和 FCM 接收现在全天可推送。
- 桌面组件2的图片区与标题区高度比例从约 2:1 调整为约 5:2，让新闻图片占据更大的上方区域，同时保持组件整体 4×3、19sp 两行标题和完整图片显示方式。
- 修复智能添加赛程遇到连续中文/英文标点时无法理解的问题；现在支持 `。。。`、`...`、省略号、问号、冒号和常见中英文引号，不会把这些符号误认为球队名称的一部分。
- 桌面组件1恢复默认显示北京时间今天的比赛；旧版默认停在“明天”的组件会自动迁移回今天，用户手动选择过其他日期的组件不受影响。
- 比赛接口请求增加三次短间隔重试，手动刷新会替换卡住的旧任务并立即显示“刷新中”，避免偶发网络波动后长期停在“更新失败”。
- 桌面组件2只保留最新一期新闻，图片区固定约占整体内容的七分之五；刷新请求绕过 CDN 旧缓存，并阻止较旧响应覆盖手机中已经缓存的新一期。
- 蓝鸟新闻工作流调整为先提交新闻 JSON、确认 GitHub Raw 已可读取，再发送 FCM 通知；App 从通知进入后也会短时自动重试，避免通知已到但正文尚未同步。
- 以后用户说“发布这个新版本到 GitHub”等同于执行完整发布流程：生成并验证固定签名 APK、创建 GitHub Release、上传安装包并更新 App 检查更新清单。
- 已使用历史固定证书生成并发布 `releases/sports-calendar-2.3.2-debug.apk`：`8,099,451` 字节，SHA-256 `F479E41EE4EB0B0400F1C808708BF4054CD93606DBB78312A9B314960E44A15D`；包名、`versionCode 44`、`versionName 2.3.2`、APK Signature Scheme v2 和证书指纹均已验证，GitHub 远程文件与本机完全一致。

## 2.3.1（已发布）

- 修复 App 内检查更新点击下载后部分手机无法打开 GitHub 下载页的问题：改由 Android 前台 Activity 通过系统浏览器选择器打开 HTTPS 下载链接，不再依赖受限的 WebView/后台 Context 跳转。
- 日历每日详情去除单场删除图标；改为连续点击同一场比赛两下，再显示原有删除确认弹窗，避免误触。
- 暂时移除 App 首页日历上方的“今日赛程”展示，桌面组件1继续默认显示明日赛程。
- 阿森纳新闻从内置页面框架升级为自动双源：GitHub Actions 每小时两次并行读取 Arsenal.com 官方文章 Sitemap 与 The Guardian 阿森纳 RSS，单源失败时保留该源旧缓存，App 再以 GitHub Raw、jsDelivr 和 APK 内置 JSON 三级回退。
- 阿森纳队徽改为 APK 内置透明 PNG，不再依赖第三方远程队徽地址。
- 组件2保持 4×3，只显示最新一条新闻；图片区域扩大并改为完整缩放，避免裁掉人物或画面边缘。远端新闻改为并行获取，手动刷新使用加急 Worker，减少等待时间。
- 新闻详情的 English/中文切换不再继承全局按钮位移、阴影或蓝色点击高亮，切换时文字位置保持不变。
- 去除新闻文章标题上方的多余内边距，标题紧接页头开始排版。
- Web 自动化测试 39 项、新闻后台测试 30 项、稳定性检查 37 项全部通过；Android Java 编译、JVM 测试和 Lint 均成功，新闻后台生产依赖审计为 0。
- 已使用历史固定证书生成并发布 `releases/sports-calendar-2.3.1-debug.apk`：`8,081,225` 字节，SHA-256 `E47E9642144AD320D2422981094BB71ED9F55D2AECD24AB8BC6BD74A87DF8BBB`；包名、`versionCode 43`、v2 签名及证书指纹均通过验证，App 更新清单已切换到 `2.3.1`。

## 2.3.0（重大更新，已发布）

- 新增“智能添加赛程”：可直接输入或语音描述比赛安排，由本地规则与 Cloudflare DeepSeek 智能代理提取日期、时间、主客队，预览确认后写入日历并同步桌面组件。
- 智能解析服务的 API 密钥只保存在 Cloudflare Worker Secret 中；客户端仅请求受限 HTTPS 接口，并有来源、请求大小、超时和频率限制。
- 日历每日详情支持单独删除任意一场比赛，并在二次确认后持久保留删除结果，刷新赛程不会重复添加同一赛事。
- 包含近期的桌面组件新闻展示、比赛改期处理、新闻阅读和推送静默等改进。
- 固定签名 APK 已发布：`8,028,202` 字节，SHA-256 `12808539B84433C1480C53C68A41CA8E93E9008630435E16CA989CF04BA98C57`；包名 `com.local.sportscalendar`、`versionCode 42`、v2 签名和历史证书均已验证。

## 2.2.19（源码完成，未打包）

- 日历每日详情中的每一场比赛新增独立删除入口，点击后显示包含对阵、日期和时间的二次确认弹窗。
- 确认删除只移除该条赛程，不影响同一天的其他比赛；日历、首页今日比赛和桌面组件会立即同步更新。
- 删除赛事 ID 会持久保存，之后刷新已导入赛程时不会把同一场比赛重新加回。
- Web 测试 34 项、稳定性检查 35 项通过；本轮未打包。

## 2.2.18（源码完成，未打包）

- 新增 Cloudflare Workers 智能赛程代理：通过受限 CORS、请求限长、10 秒上游超时及每客户端每分钟 10 次限流，安全调用 DeepSeek V4；API Key 仅以 Worker Secret 存在。
- 新闻推送新增北京时间静默时段：每天 00:00–08:59 不发送、不展示新闻通知；夜间新增新闻保留待推送状态，并在 09:00 后继续尝试，避免比赛结果剧透。
- 新增“智能添加赛程”：在侧边菜单说一句或输入一句比赛安排，先自动提取日期、开赛时间、主客队，再以确认卡片展示后写入本地日历。
- 本地理解已覆盖“阿森纳在 8 月 26 日晚上 8 点主场打赫罗纳”等常用中文表达；信息不全时明确提示补充，而不会擅自编造日期或时间。
- 自定义比赛保存在原有 IndexedDB 赛程中，标记为非托管记录，不会在刷新已导入球队赛程时被删除，并会同步给桌面赛程组件。
- 新增 Android 原生语音识别桥接，只在点击“语音输入”时请求麦克风权限；网页环境则自动使用浏览器语音识别能力。
- 已预留 HTTPS 智能解析服务接入点，API 密钥必须保存在服务端或 Secret 中，不能写入 APK；等待接入服务地址后可提升复杂口语的理解准确率。

## 2.2.17（已发布）

- 首页“今日比赛”移除总容器、左上角标题和场次数量；每场比赛独立显示为简约半透明磨砂玻璃卡片。
- 今日比赛卡片只保留双方无边框队徽和中央比分；尚未开始的比赛在相同位置显示开赛时间。
- 日历列顺序改为周一至周日，月历可见范围和首行日期计算同步改为周一首列。
- 修复新闻详情滚动位置串用：每篇文章按文章与语言独立记忆滚动位置；首次打开新文章从顶部开始，重新打开已读文章恢复到此前位置。
- Web 29 项测试和稳定性检查 31 项通过；已生成固定签名 APK（`8,028,810` 字节），SHA-256：`55A36631651B3F37845BA79AED44C37CABBCE2AE765169ADD5F08F6C3454FA8E`。
- `aapt` 已确认包名 `com.local.sportscalendar`、`versionCode 39`、`versionName 2.2.17`；APK Signature Scheme v2 和历史证书 SHA-256 均验证通过。
- GitHub Release `v2.2.17` 已发布，远程资产大小和 SHA-256 与本机 APK 完全一致；应用内更新清单已切换到本版本。

## 2.2.16（已发布）

- 新闻组件2移除会产生内缩卡片效果的 `StackView`，改为直接渲染最新一条新闻；4×3 布局中图片完整铺满上半区并保持顶部圆角，标题固定在下半区。
- 修复深圳新鹏城赛程中重庆铜梁龙、辽宁铁人等对手缺少队徽的问题：当 ESPN 不提供队徽时，按球队 ID/名称使用 CFL 中超官方队徽，并保留已知球队的内置官方地址兜底。
- 首页日历上方新增淡蓝色“今日比赛”列表，显示今天全部已导入比赛；每场独立一行，包含双方队徽、球队、时间/比分、联赛和状态，点击仍可进入当天比赛详情。
- 桌面组件1的初始日期改为明天，便于不打开 App 即可查看次日赛程；已由用户手动切换过日期的现有组件继续保留自己的选择。
- 比赛 API 检查扩充为 39 项，新增 CFL 中超官方队徽兜底通道；连续 3 轮全部成功，中超返回 240 场数据。
- 390×844 手机视口实测：今日比赛单行高度 58px、无横向溢出；深圳导入 30 场中超赛程后，重庆铜梁龙和辽宁铁人队徽均以 200×200 官方图片正常加载，控制台无警告和错误。
- Web 29 项测试、后台新闻服务 23 项测试、稳定性检查 30 项、Android 单元测试和 Lint 全部通过；`build:web` 与 `sync:android` 成功。
- 发布前依赖审计发现 `undici 7.28.0` 新增高危安全公告，已升级至 `7.29.0`；新闻后台测试重新通过，生产依赖审计恢复为 0 个漏洞。
- 使用历史固定证书生成 `releases/sports-calendar-2.2.16-debug.apk`，大小 `8,036,906` 字节，SHA-256：`43CF17D1FB9732D0F67607434C8FC7027C02E4C7D2A799556BADC2C75B8DDD7B`。
- `aapt` 已确认包名 `com.local.sportscalendar`、`versionCode 38`、`versionName 2.2.16`；APK Signature Scheme v2 和历史证书 SHA-256 均验证通过。
- GitHub Release `v2.2.16` 已发布；远程资产大小 `8,036,906` 字节，GitHub 记录的 SHA-256 与本机 APK 完全一致，应用内更新清单已切换到本版本。

## 2.2.15（源码完成，未打包）

- 移除 App 页面中所有组件、文字和交互状态的阴影；Android 两个桌面组件继续使用无 elevation 的原生布局。
- 统一冷启动、桌面图标、系统最近任务、桌面组件、通知和 App 已在后台时再次打开的开屏流程：全部从同一纯蓝背景播放 1.5 秒 GIF，并等待动画结束后再打开目标新闻。
- 原生 Android 启动页改为纯蓝过渡，不再先闪现启动图标；WebView 在首帧渲染前也使用同一蓝色，避免蓝色、米色和图标互相跳变。
- 开屏 GIF 在每次热启动时从第一帧重新播放；连续收到多个打开请求时复用同一轮动画，避免叠层和重复计时器造成显示混乱。
- 新增 `npm run check:apis`，覆盖当前 ESPN、CFL 与中国足协比赛数据接口，并验证返回结构与数据量。
- 联网检查发现英超整季单次 ESPN 请求在 3 次中有 1 次超过 15 秒；改为最多 45 天一段、最多 3 段并发并保留分段重试后，38 个检查项连续 3 轮全部成功。
- 足球球队目录为空时，会从分段赛程反推球队；中国足协 JSONP 增加两次尝试，减少短暂网络波动造成空赛程。
- 欧冠接口连接正常，但当前赛季日程和球队目录暂为 0 条；App 会把它视为“上游暂未发布数据”，不伪造赛程。
- Web 29 项测试、后台新闻服务 23 项测试、稳定性检查 29 项、Android 单元测试和 Lint 全部通过；390×844 视口实测 535 个页面元素阴影数为 0，开屏时序和控制台均正常。
- 本轮没有执行 APK 打包、签名验证或 GitHub 发布；下一安装包目标为 `2.2.15 / versionCode 37`。

## 2.2.14（已发布）

- 修复组件2同时显示多个新闻卡片窗口的问题，改为只显示最新一篇新闻。
- 新闻组件数据上限从 3 改为 1，已存在的三条旧缓存升级后也只读取最新一条。
- 关闭 `StackView` 循环显示，避免单条数据仍产生重复窗口视觉。
- 新增 Android 单元测试，验证多条新闻数据始终只保留发布时间最新的一条。
- 修复比赛改期后旧日期可能残留的问题：优先按数据源赛事 ID 迁移，数据源换 ID 时仅对唯一对阵执行兜底迁移，避免连续系列赛误合并。
- 改期后继承原比赛队徽，并保留部分刷新失败球队的关联信息，避免新日期出现空图标。
- English/中文切换改为各占完整半框的圆角滑块，切换动画更平滑且不会相互遮挡。
- 固定签名 APK 大小 `8,045,540` 字节，SHA-256：`E87511485A17F0A53EC1CE6C1727EF320632DE26F83F86F4B5EBF5FB63AFF4F6`。
- 包名 `com.local.sportscalendar`、`versionCode 36`、`versionName 2.2.14` 与历史签名证书均已校验。
- GitHub Release 已发布，Release 资产大小和 SHA-256 与本机 APK 完全一致；远程检查更新清单已切换到本版本。

## 2.2.13（源码完成，未打包）

- 修复手机新闻列表中卡片被压缩成细横条，导致图片、标题和信息标签被裁切的问题。
- 原因是全屏新闻弹窗的受限高度 Grid 将 20 个自动行压缩，而卡片自身又设置了隐藏溢出。
- 新闻列表改为使用 `grid-auto-rows: max-content`，每张卡片按自身内容高度排列并在列表内部正常滚动。
- 新增稳定性回归检查，防止后续再次移除新闻卡片的内容高度约束。
- 390×844 手机视口实测：20 条新闻正常滚动，前三张卡片高度为 146px、117px、137px，图片、标题及标签均完整显示。本轮未生成 APK。

## 2.2.12（已生成 APK）

- 恢复新闻详情正文：优先显示内置英文/中文正文，缺少英文正文时由 Android 原生能力读取 MLB 文章。
- English/中文语言切换改为圆角胶囊按钮，选中状态保持清晰。
- 新闻列表不再循环使用蓝、绿、粉背景；所有新闻卡片统一为白色背景、1px 浅色边框和轻微圆角。
- 调整手机正文间距、字号和行高，正文紧贴图片下方并保持可读性。
- Web 27 项自动化测试与 28 项稳定性检查通过；浏览器实测英文和中文正文均为 41 段。
- Android 单元测试和 Lint 通过；已生成固定签名 Debug APK，大小 `8,043,916` 字节。
- APK SHA-256：`AA81B3EDC0F4D684BDF17FABC95954EBFB014E4E7C08D78EEC153C314B9CC8E8`。
- 本版暂未发布到 GitHub Release，App 内检查更新仍指向 `2.2.11`。

## 2.2.11（已发布）

- 组件2保持 4×3，改为缓存最近三条蓝鸟新闻，并使用原生滑动卡片切换；图片区域增大、标题字号保持 19sp。
- 新闻组件图片按文章分别缓存，15 分钟刷新失败时继续使用已有内容，改善隔天切换后图片偶发消失。
- 开屏动画显示时间由 3 秒缩短为 1.5 秒。
- 日期、网站和作者拆成蓝、黄、绿三种高亮标签，首页、新闻列表和新闻详情统一使用。
- 手机新闻卡片改为紧凑 16:9 图片、标题和三项信息；详情页只保留标题、三项信息与图片，移除摘要和正文。
- 去掉新闻卡片点击后的蓝色焦点框；首页新闻只保留最新一条并收窄上下间距。
- 删除首页新闻推送开关/状态框，新闻推送默认开启；Android 13 及以上首次安装只请求一次通知权限。
- Web 27 项测试、28 项稳定性检查、Android 单元测试和 Lint 均通过。
- 已生成可覆盖安装的固定签名 Debug APK，SHA-256 为 `02D4F04C88DF9985BF746A5ED4A367344824CD2306FD6AAF75A2C214346743FD`。
- 已发布 GitHub Release，并更新远程 `public/version.json`；旧版 App 点击“检查更新”可以发现并下载本版本。

## 2.2.10（已生成 APK）

- 首次进入 App 时增加 3 秒奔跑 GIF 开屏动画，保持原有纯蓝色背景，并在结束时平滑淡出进入首页。
- 开屏 GIF 已裁剪为精确 3 秒、38 帧，体积由原素材约 5.4 MB 降至约 826 KB，降低首次加载等待。
- Android 原生启动页背景同步为同一蓝色，减少从系统启动页切换到 GIF 时的颜色跳变。
- 补齐此前遗漏的组件2新闻改造：尺寸调整为 4×3，上半部分显示无留白圆角大图，下方显示更大的两行标题。
- 组件2每 15 分钟独立更新最近一篇蓝鸟新闻，网络失败保留上次图片和标题，点击组件直接打开对应文章。
- 删除组件2旧比赛详情服务和布局，组件1的比赛刷新与组件2的新闻刷新不再相互覆盖。
- 清理 README 中已经删除的偏好、统计和文件导入说明，并新增当前双组件、新闻和固定签名工作流。
- 修复 Web 构建把跑步试验素材全部装入 APK 的问题，并避免重复签名造成包体异常膨胀；最终 APK 为约 7.7 MB。
- 新闻后台 XML 解析依赖升级到安全补丁版，生产依赖审计为 0 个漏洞。
- Debug 和 Release 构建统一要求使用固定的原始签名文件，不再允许新电脑静默生成另一张 Debug 证书。
- 打包前自动读取 keystore 证书并校验 SHA-256；与历史证书不一致时立即停止，避免到手机安装阶段才发现软件包冲突。
- 历史签名证书 SHA-256 固定为 `7EF83E3EC40B7BF1E9AAF551589EE73C378FC26F29202255F0466BCAB759BED0`。
- 已生成可覆盖安装的 `2.2.10` Debug APK，文件 SHA-256 为 `776C4290B0D4266AD886C6EB8A2EFF239A4B14A82410C6CAB1DB74285F7D0C53`。

## 2.2.9（已生成 APK）

- 手机首页新闻卡片改为 4:3 大图，图片贴合卡片顶部并加入圆角，标题字号同步放大。
- 首页新闻区域不再显示“推送已开启”、后台检查时间等长状态文字；推送状态由开关本身表示。
- 侧栏删除三个统计框、“我的偏好”和“文件导入”，只保留赛程管理、联赛导入和应用更新。
- 桌面组件的实时快照改为按日期分别保存，切到明日、后日时不会再被其他日期的刷新结果覆盖。
- 修复详情组件完成联网刷新后又被本地旧数据覆盖的问题，降低跨日期队徽变回占位图的概率。
- 已生成可覆盖安装的 `2.2.9` Debug APK。

## 2.2.8（已生成 APK）

- 手机首页新闻改为每篇占满一整行，封面在上，标题和内容概述在下，避免三列布局挤压内容。
- 新闻全文页和新闻列表支持从左右屏幕边缘向内滑动返回。
- 接入 Android 系统返回事件，vivo 的系统边缘返回手势会优先关闭全文、新闻列表或弹窗，不会直接退出 App。
- 删除“已自动同步”等成功提示；自动刷新成功后保持界面安静，只在失败或需要处理时提示。
- 已生成可覆盖安装的 `2.2.8` Debug APK。

## 2.2.7（已生成 APK）

- 新闻入口从侧栏移到主界面日历下方，默认展示最近三篇蓝鸟新闻，并可通过“查看更多”进入完整列表。
- 新闻列表简化为英文标题、内容概述、发布时间和来源；点击卡片进入独立全文阅读页。
- 全文页默认显示 MLB 英文原文，右上角可切换中文；中英文正文缓存相互隔离，切换后不会串页。
- 新闻抓取会从 MLB 官方页面提取文章封面，主页、新闻列表和全文页均可显示同一张官方图片。
- 缺少封面的旧文章会在后续自动任务中补抓；图片仅接受 MLB 官方 HTTPS 域名。
- 新闻封面统一加载 640px 版本，减少手机流量和首屏等待。
- 移除“测试通知”按钮，保留实际新闻推送开关和后台状态提示。
- 修复新闻推送偶发永久丢失：云端发送失败的文章会进入持久待重试队列，每条消息最多即时重试三次，后续定时任务继续补发。
- 修复手机后台检查连续失败三次后任务永久停止的问题；失败周期结束后仍保留下一次定时检查。
- 手机只有在系统通知真正提交成功后才记录去重；权限、通知频道或系统调用失败时保留 RSS 游标，之后继续补发。
- App 会记录并显示 FCM 主题订阅、系统通知频道、最近后台检查、最近通知和具体后台错误，打开侧栏时自动刷新诊断状态。
- 已生成可覆盖安装的 `2.2.7` Debug APK，内置 20 篇带官方封面的蓝鸟新闻。

## 2.2.6（已生成 APK）

- 接入 DeepSeek API，GitHub Actions 抓取蓝鸟新闻后自动生成中文标题、通知摘要和正文。
- 新增可维护的 MLB 球队名、棒球术语和翻译风格参考，球员姓名默认保留英文，比分与统计数字不得改写。
- App 新闻列表、正文和 FCM 通知优先显示中文；翻译不可用时自动回退英文，不影响新闻更新。
- 只翻译新文章或英文内容发生变化的文章，成功结果按来源哈希复用，避免每次定时任务重复收费。
- DeepSeek Key 只从 GitHub Secret 读取，不写入 APK、仓库、新闻文件或日志。
- Android 本地新闻检查会先等待 GitHub 中文数据；若远程任务长时间不可用，一小时后才使用英文兜底，且继续与 FCM 按文章 ID 去重。
- 首次线上翻译已成功补齐 20 篇中文标题和 20 篇中文正文；4 篇 MLB 源数据没有摘要，因此不生成虚构摘要。
- 修复部分新闻推送只有标题、没有正文的问题；没有摘要时依次使用中文正文首段、英文摘要或英文正文首段，通知收起和展开状态都能看到内容。
- 已生成可覆盖安装的 `2.2.6` Debug APK，包含 DeepSeek 中文新闻和通知正文兜底修复。

## 2.2.5（已生成 APK）

- 修复 GitHub Actions 免费定时任务严重延迟时，App 长时间看不到 MLB 新文章的问题；Android 现在会直接读取 MLB 官方 RSS。
- 修复 jsDelivr 较快但数据陈旧时覆盖 GitHub Raw 新数据的问题；多个静态源改为按最新文章发布时间选择。
- App 启动和前台同步以 MLB 直接数据为准，同时合并静态 JSON 中已经抓取好的英文正文。
- 开启新闻推送后，Android WorkManager 每 15 分钟后台检查 MLB RSS，作为 GitHub Actions 和 FCM 的本地补偿通道。
- FCM 改为高优先级数据消息，前台与后台统一由 App 创建通知，并与本地检查按文章 ID 去重。
- 升级后会自动恢复 FCM 主题订阅和后台任务；关闭推送时同步取消后台检查。
- 新闻面板新增“测试通知”，可以立即判断 vivo 的通知权限和通知频道是否正常。
- 新闻面板会显示手机后台最近检查时间及失败状态，不再只显示模糊的“已开启”。
- 已生成可覆盖安装的 `2.2.5` Debug APK；安装后建议先打开一次 App，并使用“测试通知”确认 vivo 通知权限。

## 2.2.4（已生成 APK）

- 新闻同步同时尝试 jsDelivr、GitHub Raw 和 Android 原生网络，单条线路变慢时由最快的可用线路完成。
- Android 原生新闻请求读取超时延长到 25 秒，响应仍限制为 1 MB，并严格限制到项目的两个新闻地址。
- App 每次启动自动同步，保持打开时每 5 分钟检查一次；从后台回到前台或网络恢复时也会自动检查。
- 自动同步失败时继续显示本地新闻，不用反复手动刷新；手动刷新仍会明确显示错误。
- 修正 FCM HTTP v1 的 `channel_id` 与 `click_action` 字段，解决新文章推送请求被服务器拒绝的问题。
- 新闻工作流在代码发布时使用 `validate_only` 验证 Firebase 凭据和消息格式，不向手机发送测试通知。
- Firebase Secret 兼容原始 JSON、二次引号包裹的 JSON 和 Base64 JSON；误放 `google-services.json` 或缺少服务账号字段时会在 Actions 中给出明确诊断且不泄露密钥。
- 更新后的 Firebase Admin 服务账号已通过线上 OAuth、FCM 权限和 `validate_only` 消息校验，服务端推送链路已准备就绪。
- 已生成可覆盖安装的 `2.2.4` Debug APK；远程更新下载地址将在 APK 上传到公开 HTTPS 地址后单独发布。

## 2.2.3（已生成 APK）

- GitHub Actions 现在一次抓取最近 20 篇蓝鸟新闻的英文正文，App 刷新一次即可同步标题和正文。
- 新闻卡片优先读取随新闻数据同步的正文，不再要求手机逐篇直连 MLB；旧数据没有正文时仍保留原生读取作为后备。
- 当前 20 篇新闻均已抓取正文，共 456 个段落，新闻 JSON 约 152 KB。
- FCM 推送失败不再阻断新闻文件更新，避免同一批新闻每 15 分钟重复失败并触发邮件提醒。
- GitHub Actions 的 checkout 和 setup-node 升级到 Node 24 运行时版本，移除旧 Node 20 警告。
- 为本地 CSS/JS 资源增加版本参数，避免 App 升级后 WebView 继续使用旧新闻逻辑缓存。
- 已生成可覆盖安装的 `2.2.3` Debug APK；远程更新下载地址将在 APK 上传后单独发布。

## 2.2.2（已生成 APK）

- 新闻窗口改为 100% 全屏，充分使用手机屏幕。
- 新闻卡片改为点击标题下拉/收起英文正文，不再跳转外部浏览器。
- 正文按需从 MLB 官方 AMP 页面读取，只提取纯文本段落并在当前会话缓存。
- 原生请求限制为 MLB 官方 HTTPS 地址，并增加 15 秒超时和 768 KB 大小上限。
- 已生成可覆盖安装的 `2.2.2` Debug APK；远程更新下载地址将在 APK 上传后单独发布。

## 2.2.1（已发布）

- 新增“多伦多蓝鸟新闻”，可在 App 内查看英文标题、摘要、发布时间和 MLB 原文。
- GitHub Actions 每 15 分钟检查一次蓝鸟官网 RSS，直接生成英文新闻 JSON，不调用翻译 API。
- 新增准实时英文通知开关；启用后通过 Firebase 推送新新闻，点击通知可打开对应新闻。
- 新闻列表支持手动刷新、十分钟本地缓存和断网回退。
- 第一次运行只建立新闻基线，不批量推送已有旧新闻。
- 新闻后端不再要求 Firebase Blaze、Cloud Functions 或 Firestore，公开仓库可零成本运行。
- 增加来源链接、响应大小和字段长度校验；本阶段不需要 DeepSeek Key。
- GitHub 免费新闻任务首次手动运行成功，英文新闻 JSON 已可从公网读取。
- 首个 APK 内置最近 20 条蓝鸟英文新闻，其中最近三个北京时间自然日共 15 条，首次打开即可浏览。
- 已生成包含 Firebase FCM 配置的 `2.2.1` Debug APK。

## 2.1.10（未单独发布）

- 应用图标直接使用此前确定的像素跑步小人。
- 移除图标四角的足球、篮球、棒球和橄榄球，只保留小人与浅蓝背景。
- 页面和日历保持不变。
- 桌面组件所选日期没有比赛时不再显示“今日暂无”等占位内容，比赛区域保持空白。
- 本版修改合并进入 `2.2.1`，未单独生成 APK。

## 2.1.9（已发布）

- 左拉菜单最底部新增“应用更新”组件，显示当前版本并支持手动检查更新。
- 检测到新版时展示版本号、更新说明和下载入口。
- 更新检查同时使用两个远程地址容错；断网、超时、清单错误和下载地址未发布时会给出明确提示。
- 下载入口通过 Android 系统浏览器打开，不在 App 内静默安装。
- 修复部分 MLB 等历史比赛将对象比分显示成 `[object Object]` 的问题；对象格式比分会提取真实数值，异常旧记录会先同步再显示。
- 修复赛程过多时备用存储超出容量却误报“主存储保存失败”的问题，不再向用户展示浏览器英文错误。
- 桌面组件1移除底部三个日期圆点，只保留顶部前一天、后一天按钮。
- 加大并拉开左右日期按钮，刷新时间移动到刷新按钮旁边，比赛列表获得更多纵向空间。

## 2.1.8（已发布）

- 重做桌面组件刷新：后台任务由 Android 可靠调度，失败会自动重试，并显示上次更新时间或更新失败。
- 修复中冠等比赛“有比分就误判已结束”的问题，延期和取消状态也会单独显示。
- 多个桌面组件可以各自停留在不同日期，不再互相改变。
- 本地赛程加入自动备份和损坏恢复，旧版本数据会自动迁移。
- 打开 App 时不再立即加载完整球队列表和大量队徽，减少首屏等待。
- 全部球队更新改为并行限流，等待时间更短，部分接口失败时继续保留原赛程。
- 世界杯赛期和中冠年度配置改为自动计算，减少每年必须发布新版的情况。
- 文件导入增加大小、数量、日期和颜色安全检查。
- 加强网络、图片和 WebView 安全限制，减少超大响应造成的卡顿风险。
- 清理项目依赖和生成文件，加入正式签名发布流程，为后续稳定更新做准备。

## 2.1.7（已发布）

- 日历比赛弹窗刷新时只更新比分和状态，不再重建队徽节点，减少打开、关闭和刷新时的图标闪烁。
- 提前预加载当前月份队徽，弹窗中的队徽改为优先加载，并保留已有的非空队徽资源。
- 启动页与 WebView 背景改为应用的浅米色，减少打开 App 时持续 1—2 秒的白屏感。
- 打开 App 后后台预刷新今天和昨天的比赛；未验证的历史 `0 - 0` 会先显示“正在同步”，避免先展示错误比分再跳变。
- 桌面组件日期切换改为先显示本地数据、再后台刷新，并阻止慢请求覆盖已经切换的新日期。
- 放大组件日期按钮点击区域，底部加入过去、今天、未来三个圆点导航。
- 中超改用 ESPN `chn.1` 赛程与实时比分，旧 CFL 中文球队可在点击“更新”后自动迁移。
- 组件刷新加入中超 ESPN 实时比分兜底，修复正在比赛时比分不更新、刷新无响应的问题。

## 2.1.6（已发布）

- 修复未来比赛被误显示为“进行中”的问题。
- 比赛开始时间还在未来时，网页日历弹窗和桌面组件都会强制显示为未开始状态。
- 开赛时间文本例如 `07:10`、`7:10 PM` 不再被误判成实时比赛状态。
- 优化 App 内队徽图片加载：远程队徽会在本地缓存，下一次打开加载更快。
- App 启动图标改为像素小人，并在四角加入足球、篮球、棒球和橄榄球元素。
- 侧边栏标题框只保留“体育迷日历”五个字。
- 桌面组件1改为按日期切换比赛列表，左右箭头可切到前一天/后一天，组件2同步跟随所选日期。

## 2.1.5（已发布）

- 修复 vivo 桌面图标显示成默认图标和外框的问题。
- 将启动图标替换为观赛日记卡通蓝色 App logo。
- 移除 Android adaptive icon 分层配置，避免系统再套用默认白底/外框。
- 修复桌面组件比赛行的队徽和浅色卡片框偶尔不显示的问题，所有联赛的比赛行都走同一套可见性保护。

## 2.1.4（已发布）

- 中超、中甲、中乙切换到 CFL 官方赛程数据源。
- 修复中乙比分更新不及时、赛中比分不变红的问题。
- 扩展所有联赛的赛中/完赛状态识别，减少“未开始”“0-0”“已结束”显示不一致的问题。
- 点击桌面组件空白区域也可以进入 App。
- 原 4x4 桌面组件命名为“观赛日记 组件1”。
- 新增 2x2 桌面组件“观赛日记 组件2”，一次显示一场比赛，支持滑动切换，展示队徽、队名、比分、状态、联赛和场馆信息。
- 组件1 和组件2 共用同一份关注比赛数据、比分刷新和队徽缓存。

## 2.1.3（已发布）

- 新增 NFL 赛程导入。
- 优化 NBA、MLB、NFL 的新赛季判断，更新时会尝试抓取当前赛季和下一赛季。
- 修复足球、篮球、棒球、橄榄球的主客队展示顺序。
- 未开始或暂无比分的比赛统一显示为 `0 - 0`。
- 桌面组件补充 MLB 局数信息，例如“5局上”“8局下”。
- 桌面组件今日比赛列表支持超过三场时翻动查看。
- 加入 App logo 候选图资源。

## 2.1.2（已发布）

- App 首页改为优先展示日历，打开后更快看到赛程。
- 导入、更新、删除、筛选等操作整理到左侧侧边栏。
- 支持从左向右滑动或点击左上角按钮打开侧边栏。
- 调整移动端布局，减少日历和赛程卡片溢出。

## 2.1.1（已发布）

- 修复安装后联赛图标、球队图标可能不显示的问题。
- 将主要联赛图标改为本地随 App 打包，不再完全依赖启动时联网加载。
- 优化图标路径和 Android WebView 的图片加载兼容性。

## 2.1.0（已发布）

- 将原本的本地静态网页包装成 Android App。
- 支持在手机上安装使用“观赛日记”。
- 支持导入关注球队的赛程，并在月历中查看。
- 支持桌面小组件显示今日关注比赛。
- 支持手动刷新小组件比分。
- 保留本地数据存储，未登录、无服务器也能使用。

## 2.0.0（App 打包准备版，未单独发布）

- 完成 Android App 打包前的整体整理。
- 赛程数据结构更稳定，一场比赛可以同时关联多支已关注球队。
- 更新赛程时更安全：拉取失败不会轻易清空旧赛程。
- 桌面小组件支持读取今日关注比赛，并可手动刷新比分。
- 小组件增加队徽缓存和最近一次比分快照，离线或重启后显示更稳定。
- 导入赛程改为按球队导入完整已确定赛程，不再要求手动选择日期范围。
- 文件导入增强，支持 `.ics`、`.csv`、`.json`。

## 1.7.0（桌面小组件初版）

- 新增 Android 桌面小组件。
- 小组件按北京时间显示“今日关注比赛”。
- 小组件一共显示三行比赛，超过三场可翻动查看。
- 小组件支持刷新按钮，用于手动检查最新比分。
- 比赛卡片显示双方队徽、开赛时间、比分、状态和联赛名。

## 1.6.0（Android App 初步包装）

- 使用 Capacitor 将静态网页接入 Android 工程。
- 应用显示名确定为“观赛日记”。
- 支持生成 Debug APK，并可通过 `adb install` 安装到 vivo 手机测试。
- App 内继续使用本地数据存储，浏览器数据不会自动迁移到 App。

## 1.5.0（赛程管理增强）

- 新增“删除”功能，可以删除已导入球队的赛程。
- 新增“更新”功能，可以一次刷新所有已关注球队的赛程。
- 更新时会自动拉取该球队目前已确定的全部赛程。
- 日历里同一天有多场比赛时改为全部显示，行高随比赛数量自动适配。
- 未开始比赛统一显示为 `0 - 0`。

## 1.4.0（更多联赛与数据源）

- 新增世界杯赛程导入。
- 世界杯球队列表固定显示 48 支参赛队。
- 加入中超、中甲、中乙、中冠等中国足球联赛。
- 英超支持 26-27 赛季赛程。
- 支持 MLB、NBA、英超等常用联赛的球队赛程导入。

## 1.3.0（日历与比赛详情）

- 月历改为主要视图，打开后优先看到赛程。
- 支持按月份前后切换。
- 点击某一天会弹出当天具体比赛信息。
- 弹窗内以类似桌面组件的样式展示双方队徽、时间、比分和联赛。
- 日历格子中不再只挤队徽，查看具体比赛更清楚。

## 1.2.0（球队导入与队徽）

- 导入方式改为先选择联赛，再选择具体球队。
- 一次只导入一支球队的赛程，不再默认导入整个联赛。
- 球队选择区加入队徽。
- 日历中的对阵双方也显示队徽。
- 不同球队或赛事使用不同颜色区分。

## 1.1.0（界面和移动端优化）

- 调整为新粗野派视觉风格。
- 颜色改为更柔和、饱和度更低，同时保留清晰对比。
- 优化移动端尺寸，减少内容溢出。
- 日历边框和比赛卡片尺寸更适合手机屏幕。

## 1.0.0（静态网页初版）

- 完成体育迷日历的本地静态网页版本。
- 支持直接打开 `index.html` 使用，不需要服务器。
- 支持按月份查看体育赛程。
- 支持基础赛程导入和本地保存。
- 支持关键词关注球队。

## 使用提醒

- 当前 APK 是个人测试用 Debug 包，不是应用商店正式签名包。
- 手机已安装版本只有在重新打包并安装新 APK 后才会更新。
- 比分实时性取决于对应联赛数据源是否及时更新；App 会尽量刷新，但不能凭空生成数据源没有提供的真实比分。
