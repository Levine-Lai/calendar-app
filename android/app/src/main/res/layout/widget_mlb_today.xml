<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:padding="10dp">

    <LinearLayout
        android:id="@+id/widget_toolbar"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_date_label"
            style="@style/WidgetDateText" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/widget_prev_day_button"
                android:layout_width="38dp"
                android:layout_height="34dp"
                android:layout_marginEnd="10dp"
                android:background="@android:color/transparent"
                android:contentDescription="@string/widget_prev_day"
                android:padding="4dp"
                android:scaleType="centerInside"
                android:src="@drawable/ic_widget_chevron_left" />

            <ImageButton
                android:id="@+id/widget_next_day_button"
                android:layout_width="38dp"
                android:layout_height="34dp"
                android:background="@android:color/transparent"
                android:contentDescription="@string/widget_next_day"
                android:padding="4dp"
                android:scaleType="centerInside"
                android:src="@drawable/ic_widget_chevron_right" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginStart="8dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/widget_refresh_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:paddingEnd="3dp"
                android:textColor="#6A625A"
                android:textSize="11sp" />

            <ImageButton
                android:id="@+id/widget_refresh_button"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:background="@android:color/transparent"
                android:contentDescription="@string/widget_refresh"
                android:padding="3dp"
                android:scaleType="centerInside"
                android:src="@drawable/ic_widget_refresh" />
        </LinearLayout>
    </LinearLayout>

    <ListView
        android:id="@+id/game_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:divider="@android:color/transparent"
        android:dividerHeight="5dp"
        android:overScrollMode="never"
        android:scrollbars="none" />
</LinearLayout>
