<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:padding="10dp">

    <LinearLayout
        android:id="@+id/widget_toolbar"
        android:layout_width="match_parent"
        android:layout_height="28dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_date_label"
            style="@style/WidgetDateText" />

        <ImageButton
            android:id="@+id/widget_scroll_up"
            android:layout_width="20dp"
            android:layout_height="24dp"
            android:background="@android:color/transparent"
            android:contentDescription="@string/widget_scroll_up"
            android:padding="3dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_widget_chevron_up" />

        <ImageButton
            android:id="@+id/widget_scroll_down"
            android:layout_width="20dp"
            android:layout_height="24dp"
            android:background="@android:color/transparent"
            android:contentDescription="@string/widget_scroll_down"
            android:padding="3dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_widget_chevron_down" />

        <ImageButton
            android:id="@+id/widget_refresh_button"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="@android:color/transparent"
            android:contentDescription="@string/widget_refresh"
            android:padding="2dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_widget_refresh" />
    </LinearLayout>

    <ListView
        android:id="@+id/game_list"
        android:layout_width="match_parent"
        android:layout_height="196dp"
        android:clipToPadding="false"
        android:divider="@android:color/transparent"
        android:dividerHeight="5dp"
        android:overScrollMode="never"
        android:scrollbars="none" />
</LinearLayout>
