<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/detail_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_news_card_background"
    android:clipToOutline="true">

    <StackView
        android:id="@+id/news_widget_stack"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:animateFirstView="false"
        android:loopViews="false"
        android:overScrollMode="never" />

    <TextView
        android:id="@+id/news_widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:padding="20dp"
        android:text="@string/widget_news_loading"
        android:textColor="#16120F"
        android:textSize="16sp" />
</FrameLayout>
